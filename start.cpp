#include <iostream>
using namespace std;
int main()
    //this p[rogram will perform basic arithmatic operations
{
    int a, b, sum,sub;
    cout << "Enter the two numbers you want to add: ";
    cin >> a >> b;
    sum = (a + b);
    cout << "The addition of " << a << " and " << b << "are: " << sum;
sub=a-b;
    cout "enter numbers to subtract"<<endl;
    cin>>a>>b;
    cout<<sub;
    return 0;
}
